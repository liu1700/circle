# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index

GET		/f/feeds 						Feed.GetFeeds
GET 	/f/feed/:feedId 				Feed.GetFeed
POST 	/f/feed 						Feed.CreateFeed

#GET 	/c/:feedId 						Comment.GetComments
#POST 	/c/:feedId 						Comment.PostComment

# User registration
POST /u/registry/:device/:smscode  		User.Registry

# User sign in
POST /u/signin   						User.SignIn

# Sign out
POST /u/signout  						 User.SignOut
# Send sms code to the phone
POST /u/smscode                          User.SendCode

#Update user name
POST /u/update/nickname					 User.UpdateNickname
#Update user avatar

POST    /image/upload/                             Image.Upload

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
